		MOV		R0, #13 ; Move the number decimal 13 into R0
		MOV		R1, #4 ; Move the number 4 into R1
		mov		R2, #0x13 ; Move hexadecimal 13 into R2
		
		;		blank lines are ignored
		;		On any line a ; and all characters after this are ignored
		MOV		R3, #100
		MOV		r5, r2  ; this MOV has a register, not a numeric literal, as source operand
		Mov		R6, R5
		MOV		R5, R3
